pipeline {
    agent any
    environment {
        DOCKER_HUB_CREDS = credentials("DOCKER_HUB_CREDS")
        
    }
    stages {
        stage('Set Up') {
            steps {
                sh "jenkins/setup.sh"
            }
        }
        stage('Build') {
            steps {
               sh "jenkins/build.sh"
            }
        }
        stage('Test') {
            steps {
                sh "jenkins/test.sh"
            }
        }
        stage('Deploy') {
            steps {
                sh "jenkins/deploy.sh"
            }
        }
    }
}